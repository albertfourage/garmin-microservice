# Microservice security
API_KEY=your-long-random-key

# Time zone
TZ=Europe/Bratislava

# Optional: first-time credentials (only for local bootstrap, not Railway)
GARMIN_EMAIL=you@example.com
GARMIN_PASSWORD=your_password

# Preferred: paste tokens JSON blob here after local bootstrap
# GARMINTOKENS_JSON={"sso_guid":"...","access_token":"...","refresh_token":"...","expires_at": ...}

# Token file path inside container (persist if you attach a volume at /data)
GARMINTOKENS_PATH=/data/garmintokens.json
